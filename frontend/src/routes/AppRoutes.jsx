import { Routes, Route } from "react-router-dom";

// Placeholder — routes will be defined in later phases
function AppRoutes() {
  return (
    <Routes>
      <Route path="*" element={<div>FITOS — Coming Soon</div>} />
    </Routes>
  );
}

export default AppRoutes;
