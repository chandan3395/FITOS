# ─── Frontend-only ──────────────────────────────────────────
# Only VITE_-prefixed variables are exposed to the client bundle.
# All backend secrets (Mongo, JWT, OAuth, Cloudinary, WhatsApp, Sentry)
# live in backend/.env — never duplicate them here.

# Set to "false" to temporarily hide the "Continue with Google" CTA on
# the login page. Backend must also set ENABLE_GOOGLE_AUTH=false so the
# OAuth routes return 503. Defaults to enabled.
VITE_ENABLE_GOOGLE_AUTH=true

# Base URL for the backend API. Leave UNSET in local development so the app
# uses the relative "/api" path served by the Vite dev proxy (vite.config.js).
# In production (e.g. Vercel), where the frontend and backend are on separate
# origins, set this to the full backend API base URL, including the /api suffix.
# Example: VITE_API_URL=https://fitos-backend.onrender.com/api
# VITE_API_URL=
